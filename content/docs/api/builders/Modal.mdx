---
title: "Modal"
---
                 
### Modal

Represents a modal for user interactions.

<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">components</code>
  <code className="text-fd-muted-foreground">{"ActionRow<T>[]"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">data</code>
  <code className="text-fd-muted-foreground">{"Partial<APIModalInteractionResponseCallbackData>"}</code>
</div>

- Optional data for the modal.

- param:
data - Optional data for the modal.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">addComponents</code>
  <code className="text-fd-muted-foreground">{"(...components: RestOrArray<ActionRow<T>>) => Modal<T>"}</code>
</div>

Adds components to the modal.

- param:
components - Components to be added to the modal.
- returns:
The current Modal instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setComponents</code>
  <code className="text-fd-muted-foreground">{"(component: ActionRow<T>[]) => Modal<T>"}</code>
</div>

Set the components to the modal.

- param:
component - The components to set into the modal.
- returns:
The current Modal instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setTitle</code>
  <code className="text-fd-muted-foreground">{"(title: string) => Modal<T>"}</code>
</div>

Sets the title of the modal.

- param:
title - The title of the modal.
- returns:
The current Modal instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setCustomId</code>
  <code className="text-fd-muted-foreground">{"(id: string) => Modal<T>"}</code>
</div>

Sets the custom ID of the modal.

- param:
id - The custom ID for the modal.
- returns:
The current Modal instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">run</code>
  <code className="text-fd-muted-foreground">{"(func: ModalSubmitCallback) => Modal<T>"}</code>
</div>

Sets the callback function to be executed when the modal is submitted.

- param:
func - The callback function.
- returns:
The current Modal instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">toJSON</code>
  <code className="text-fd-muted-foreground">{"() => APIModalInteractionResponseCallbackData"}</code>
</div>

Converts the modal to JSON format.

- returns:
The modal data in JSON format.

</div></div>

### TextInput

Represents a text input component builder.

<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setStyle</code>
  <code className="text-fd-muted-foreground">{"(style: TextInputStyle) => TextInput"}</code>
</div>

Sets the style of the text input.

- param:
style - The style of the text input.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setLabel</code>
  <code className="text-fd-muted-foreground">{"(label: string) => TextInput"}</code>
</div>

Sets the label of the text input.

- param:
label - The label of the text input.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setPlaceholder</code>
  <code className="text-fd-muted-foreground">{"(placeholder: string) => TextInput"}</code>
</div>

Sets the placeholder of the text input.

- param:
placeholder - The placeholder text.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setLength</code>
  <code className="text-fd-muted-foreground">{"({ max, min }: Partial<OptionValuesLength>) => TextInput"}</code>
</div>

Sets the length constraints for the text input.

- param:
options - The length constraints.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setCustomId</code>
  <code className="text-fd-muted-foreground">{"(id: string) => TextInput"}</code>
</div>

Sets the custom ID of the text input.

- param:
id - The custom ID for the text input.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setValue</code>
  <code className="text-fd-muted-foreground">{"(value: string) => TextInput"}</code>
</div>

Sets the initial value of the text input.

- param:
value - The initial value.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setRequired</code>
  <code className="text-fd-muted-foreground">{"(required?: boolean) => TextInput"}</code>
</div>

Sets whether the text input is required.

- param:
required - Indicates whether the text input is required.
- returns:
The current TextInput instance.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">data</code>
  <code className="text-fd-muted-foreground">{"Partial<APITextInputComponent>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">toJSON</code>
  <code className="text-fd-muted-foreground">{"() => APITextInputComponent"}</code>
</div>

No Description



</div></div>