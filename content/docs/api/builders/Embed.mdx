---
title: "Embed"
---
                 
### Embed

Represents a message embed.

<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">data</code>
  <code className="text-fd-muted-foreground">{"Partial<APIEmbed>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setAuthor</code>
  <code className="text-fd-muted-foreground">{"(author?: { name: string; url?: string | undefined; iconUrl?: string | undefined; proxyIconUrl?: string | undefined; } | undefined) => Embed"}</code>
</div>

Sets the author of the embed.

- param:
author - The author information.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setAuthor({ name: 'John Doe', iconUrl: 'https://example.com/avatar.png' });
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setColor</code>
  <code className="text-fd-muted-foreground">{"(color?: ColorResolvable | undefined) => Embed"}</code>
</div>

Sets the color of the embed.

- param:
color - The color of the embed.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setColor('#FF0000');
embed.setColor('Blurple');
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setDescription</code>
  <code className="text-fd-muted-foreground">{"(desc?: string | undefined) => Embed"}</code>
</div>

Sets the description of the embed.

- param:
desc - The description of the embed.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setDescription('This is the description of the embed');
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">addFields</code>
  <code className="text-fd-muted-foreground">{"(...fields: RestOrArray<APIEmbedField>) => Embed"}</code>
</div>

Adds one or more fields to the embed.

- param:
fields - The fields to add to the embed.
- returns:
The updated Embed instance.
- example:
```typescript
embed.addFields({ name: 'Field 1', value: 'Value 1' }, { name: 'Field 2', value: 'Value 2' });
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setFields</code>
  <code className="text-fd-muted-foreground">{"(fields?: APIEmbedField[] | undefined) => Embed"}</code>
</div>

Sets the fields of the embed.

- param:
fields - The fields of the embed.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setFields([{ name: 'Field 1', value: 'Value 1' }, { name: 'Field 2', value: 'Value 2' }]);
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setFooter</code>
  <code className="text-fd-muted-foreground">{"(footer?: { iconUrl?: string | undefined; text: string; } | undefined) => Embed"}</code>
</div>

Sets the footer of the embed.

- param:
footer - The footer information.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setFooter({ text: 'This is the footer', iconUrl: 'https://example.com/footer.png' });
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setImage</code>
  <code className="text-fd-muted-foreground">{"(url?: string | undefined) => Embed"}</code>
</div>

Sets the image of the embed.

- param:
url - The URL of the image.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setImage('https://example.com/image.png');
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setTimestamp</code>
  <code className="text-fd-muted-foreground">{"(time?: string | number | Date) => Embed"}</code>
</div>

Sets the timestamp of the embed.

- param:
time - The timestamp value.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setTimestamp();
embed.setTimestamp(1628761200000);
embed.setTimestamp(new Date());
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setTitle</code>
  <code className="text-fd-muted-foreground">{"(title?: string | undefined) => Embed"}</code>
</div>

Sets the title of the embed.

- param:
title - The title of the embed.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setTitle('This is the title');
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setURL</code>
  <code className="text-fd-muted-foreground">{"(url?: string | undefined) => Embed"}</code>
</div>

Sets the URL of the embed.

- param:
url - The URL of the embed.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setURL('https://seyfert.com');
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">setThumbnail</code>
  <code className="text-fd-muted-foreground">{"(url?: string | undefined) => Embed"}</code>
</div>

Sets the thumbnail of the embed.

- param:
url - The URL of the thumbnail.
- returns:
The updated Embed instance.
- example:
```typescript
embed.setThumbnail('https://example.com/thumbnail.png');
```

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">toJSON</code>
  <code className="text-fd-muted-foreground">{"() => APIEmbed"}</code>
</div>

Converts the Embed instance to a JSON object.

- returns:
The JSON representation of the MessageEmbed instance.

</div></div>