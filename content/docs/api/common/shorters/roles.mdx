---
title: "Roles"
---
                 
### RoleShorter



<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">create</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, body: RESTPostAPIGuildRoleJSONBody, reason?: string | undefined) => Promise<GuildRole>"}</code>
</div>

Creates a new role in the guild.

- param:
reason The reason for creating the role.
- returns:
A Promise that resolves when the role is created.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">fetch</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, roleId: string, force?: boolean) => Promise<GuildRole>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">raw</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, roleId: string, force?: boolean) => Promise<APIRole>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">list</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, force?: boolean) => Promise<GuildRole[]>"}</code>
</div>

Retrieves a list of roles in the guild.

- param:
force Whether to force fetching roles from the API even if they exist in the cache.
- returns:
A Promise that resolves to an array of roles.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">listRaw</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, force?: boolean) => Promise<APIRole[]>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">edit</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, roleId: string, body: RESTPatchAPIGuildRoleJSONBody, reason?: string | undefined) => Promise<GuildRole>"}</code>
</div>

Edits a role in the guild.

- param:
reason The reason for editing the role.
- returns:
A Promise that resolves when the role is edited.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">delete</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, roleId: string, reason?: string | undefined) => Promise<GuildRole>"}</code>
</div>

Deletes a role from the guild.

- param:
reason The reason for deleting the role.
- returns:
A Promise that resolves when the role is deleted.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">editPositions</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, body: RESTPatchAPIGuildRolePositionsJSONBody) => Promise<GuildRole[]>"}</code>
</div>

Edits the positions of roles in the guild.

- param:
body The data to update the positions of roles with.
- returns:
A Promise that resolves to an array of edited roles.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">client</code>
  <code className="text-fd-muted-foreground">{"UsingClient"}</code>
</div>

No Description



</div></div>