---
title: "Members"
---
                 
### MemberShorter



<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">resolve</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, resolve: GuildMemberResolvable) => Promise<GuildMember | undefined>"}</code>
</div>

Resolves a member in the guild based on the provided GuildMemberResolvable.

- param:
resolve The GuildMemberResolvable to resolve.
- returns:
A Promise that resolves to the resolved member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">search</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, query?: RESTGetAPIGuildMembersSearchQuery | undefined) => Promise<GuildMember[]>"}</code>
</div>

Searches for members in the guild based on the provided query.

- param:
query The query parameters for searching members.
- returns:
A Promise that resolves to an array of matched members.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">unban</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, reason?: string | undefined) => Promise<never>"}</code>
</div>

Unbans a member from the guild.

- param:
reason The reason for unbanning the member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">ban</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, body?: RESTPutAPIGuildBanJSONBody | undefined, reason?: string | undefined) => Promise<void>"}</code>
</div>

Bans a member from the guild.

- param:
reason The reason for banning the member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">kick</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, reason?: string | undefined) => Promise<void>"}</code>
</div>

Kicks a member from the guild.

- param:
reason The reason for kicking the member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">edit</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, body: RESTPatchAPIGuildMemberJSONBody, reason?: string | undefined) => Promise<GuildMember>"}</code>
</div>

Edits a member in the guild.

- param:
reason The reason for editing the member.
- returns:
A Promise that resolves to the edited member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">add</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, body: RESTPutAPIGuildMemberJSONBody) => Promise<GuildMember | undefined>"}</code>
</div>

Adds a member to the guild.

- param:
body The request body for adding the member.
- returns:
A Promise that resolves to the added member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">fetch</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, force?: boolean) => Promise<GuildMember>"}</code>
</div>

Fetches a member from the guild.

- param:
force Whether to force fetching the member from the API even if it exists in the cache.
- returns:
A Promise that resolves to the fetched member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">raw</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, force?: boolean) => Promise<APIGuildMember>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">list</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, query?: RESTGetAPIGuildMembersQuery | undefined, force?: boolean) => Promise<GuildMember[]>"}</code>
</div>

Lists members in the guild based on the provided query.

- param:
force Whether to force listing members from the API even if they exist in the cache.
- returns:
A Promise that resolves to an array of listed members.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">addRole</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, id: string) => Promise<never>"}</code>
</div>

Adds a role to a guild member.

- param:
id The ID of the role to add.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">removeRole</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, id: string) => Promise<never>"}</code>
</div>

Removes a role from a guild member.

- param:
id The ID of the role to remove.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">listRoles</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, force?: boolean) => Promise<GuildRole[]>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">sortRoles</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, force?: boolean) => Promise<GuildRole[]>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">permissions</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, force?: boolean) => Promise<PermissionsBitField>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">presence</code>
  <code className="text-fd-muted-foreground">{"(memberId: string) => ReturnCache<(Omit<GatewayPresenceUpdate, \"user\"> & { id: string; user_id: string; } & { guild_id: string; }) | undefined>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">voice</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: (string & {}) | \"@me\", force?: boolean) => Promise<VoiceState>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">timeout</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, time: number | null, reason?: string | undefined) => Promise<GuildMember>"}</code>
</div>

Timeouts a member from the guild.

- param:
reason The reason for the timeout.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">hasTimeout</code>
  <code className="text-fd-muted-foreground">{"(member: Partial<GuildMember> | APIGuildMember) => number | false"}</code>
</div>

Checks if a member has a timeout.

- param:
member The member to check.
- returns:
The time left until the timeout expires, in milliseconds, or false if the member does not have a timeout.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">client</code>
  <code className="text-fd-muted-foreground">{"UsingClient"}</code>
</div>

No Description



</div></div>