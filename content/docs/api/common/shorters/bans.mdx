---
title: "Bans"
---
                 
### BanShorter



<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">bulkCreate</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, body: RESTPostAPIGuildBulkBanJSONBody, reason?: string | undefined) => Promise<RESTPostAPIGuildBulkBanResult>"}</code>
</div>

Bulk creates bans in the guild.

- param:
reason The reason for bulk banning members.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">remove</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, reason?: string | undefined) => Promise<never>"}</code>
</div>

Unbans a member from the guild.

- param:
reason The reason for unbanning the member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">create</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, memberId: string, body?: RESTPutAPIGuildBanJSONBody | undefined, reason?: string | undefined) => Promise<void>"}</code>
</div>

Bans a member from the guild.

- param:
reason The reason for banning the member.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">fetch</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, userId: string, force?: boolean) => Promise<GuildBan>"}</code>
</div>

Fetches a ban from the guild.

- param:
force Whether to force fetching the ban from the API even if it exists in the cache.
- returns:
A Promise that resolves to the fetched ban.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">list</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, query?: RESTGetAPIGuildBansQuery | undefined, force?: boolean) => Promise<GuildBan[]>"}</code>
</div>

Lists bans in the guild based on the provided query.

- param:
force Whether to force listing bans from the API even if they exist in the cache.
- returns:
A Promise that resolves to an array of listed bans.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">client</code>
  <code className="text-fd-muted-foreground">{"UsingClient"}</code>
</div>

No Description



</div></div>