---
title: "Webhook"
---
                 
### WebhookShorter



<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">create</code>
  <code className="text-fd-muted-foreground">{"(channelId: string, body: RESTPostAPIChannelWebhookJSONBody) => Promise<Webhook>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">delete</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, options: Partial<{ token: string; reason: string; }>) => Promise<never>"}</code>
</div>

Deletes a webhook.

- param:
options The optional parameters including token and reason.
- returns:
A Promise that resolves when the webhook is deleted.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">edit</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, body: RESTPatchAPIWebhookJSONBody | RESTPatchAPIWebhookWithTokenJSONBody, options: Partial<...>) => Promise<...>"}</code>
</div>

Edits a webhook.

- param:
options The optional parameters including token and reason.
- returns:
A Promise that resolves when the webhook is edited.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">fetch</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, token?: string | undefined) => Promise<Webhook>"}</code>
</div>

Fetches a webhook.

- param:
token The token of the webhook (optional).
- returns:
A Promise that resolves to the fetched webhook.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">writeMessage</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, token: string, { body: data, ...payload }: { body: Omit<RESTPostAPIWebhookWithTokenJSONBody, \"components\" | \"embeds\" | \"poll\"> & SendResolverProps; query?: RESTPostAPIWebhookWithTokenQuery | undefined; }) => Promise<...>"}</code>
</div>

Writes a message using the webhook.

- param:
data The data for writing the message.
- returns:
A Promise that resolves to the written message.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">editMessage</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, token: string, { messageId, body: data, ...json }: { body: Omit<RESTPatchAPIWebhookWithTokenMessageJSONBody, \"components\" | \"embeds\" | \"poll\"> & ResolverProps; messageId: string; query?: RESTGetAPIWebhookWithTokenMessageQuery | undefined; }) => Promise<...>"}</code>
</div>

Edits a message sent by the webhook.

- param:
data The data for editing the message.
- returns:
A Promise that resolves to the edited message.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">deleteMessage</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, token: string, messageId: string, reason?: string | undefined) => Promise<never>"}</code>
</div>

Deletes a message sent by the webhook.

- param:
reason The reason for deleting the message.
- returns:
A Promise that resolves when the message is deleted.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">fetchMessage</code>
  <code className="text-fd-muted-foreground">{"(webhookId: string, token: string, messageId: string, threadId?: string | undefined) => Promise<WebhookMessage>"}</code>
</div>

Fetches a message sent by the webhook.

- param:
threadId The ID of the thread the message belongs to.
- returns:
A Promise that resolves to the fetched message

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">listFromGuild</code>
  <code className="text-fd-muted-foreground">{"(guildId: string) => Promise<Webhook[]>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">listFromChannel</code>
  <code className="text-fd-muted-foreground">{"(channelId: string) => Promise<Webhook[]>"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">client</code>
  <code className="text-fd-muted-foreground">{"UsingClient"}</code>
</div>

No Description



</div></div>

### WebhookShorterOptionalParams



<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">token</code>
  <code className="text-fd-muted-foreground">{"string"}</code>
</div>

No Description



</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">reason</code>
  <code className="text-fd-muted-foreground">{"string"}</code>
</div>

No Description



</div></div>