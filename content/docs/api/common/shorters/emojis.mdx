---
title: "Emojis"
---
                 
### EmojiShorter



<div className='*:border-b [&>*:last-child]:border-b-0'><div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">list</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, force?: boolean) => Promise<GuildEmoji[]>"}</code>
</div>

Retrieves a list of emojis in the guild.

- param:
force Whether to force fetching emojis from the API even if they exist in the cache.
- returns:
A Promise that resolves to an array of emojis.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">create</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, body: Omit<RESTPostAPIGuildEmojiJSONBody, \"image\"> & { image: ImageResolvable; }) => Promise<GuildEmoji>"}</code>
</div>

Creates a new emoji in the guild.

- param:
body The data for creating the emoji.
- returns:
A Promise that resolves to the created emoji.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">fetch</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, emojiId: string, force?: boolean) => Promise<GuildEmoji>"}</code>
</div>

Fetches an emoji by its ID.

- param:
force Whether to force fetching the emoji from the API even if it exists in the cache.
- returns:
A Promise that resolves to the fetched emoji.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">delete</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, emojiId: string, reason?: string | undefined) => Promise<void>"}</code>
</div>

Deletes an emoji from the guild.

- param:
reason The reason for deleting the emoji.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">edit</code>
  <code className="text-fd-muted-foreground">{"(guildId: string, emojiId: string, body: RESTPatchAPIGuildEmojiJSONBody, reason?: string | undefined) => Promise<GuildEmoji>"}</code>
</div>

Edits an emoji in the guild.

- param:
reason The reason for editing the emoji.
- returns:
A Promise that resolves to the edited emoji.

</div>
<div className='text-sm text-fd-muted-foreground py-4'>

<div className="flex flex-row items-center gap-4">
  <code className="text-sm">client</code>
  <code className="text-fd-muted-foreground">{"UsingClient"}</code>
</div>

No Description



</div></div>